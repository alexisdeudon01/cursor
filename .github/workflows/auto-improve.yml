name: Auto-Improve Project (Thebestclient)

on:
  # GitHub Actions "schedule" runs from the repository default branch (main).
  # This workflow checks out and updates the dev branch on a schedule.
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
  # Keep this workflow definition compatible with dev/main by also supporting
  # running on dev changes (useful when iterating on agents/assets).
  push:
    branches:
      - dev
    paths:
      - '.cursor/agents/**'
      - 'Assets/Scripts/**'
      - 'Assets/Scenes/**'
      - 'Assets/Prefabs/**'

jobs:
  improve:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository (dev)
        uses: actions/checkout@v4
        with:
          ref: dev
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -q requests mermaid-cli || echo "mermaid-cli non disponible, utilisation API en ligne"

      - name: Run Auto-Improve Script with AI
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          # Essayer d'abord avec IA, sinon fallback sur script basique
          if [ -n "$ANTHROPIC_API_KEY" ]; then
            echo "🤖 Utilisation de l'IA Claude pour amélioration..."
            python3 .github/scripts/auto-improve-ai.py
          else
            echo "⚠️ Mode basique (pas d'IA) - configurez ANTHROPIC_API_KEY pour activer l'IA"
            python3 .github/scripts/auto-improve.py
          fi

      - name: Verify Metrics Quality
        run: |
          echo "📊 Vérification des métriques de qualité..."
          python3 .github/scripts/verify-metrics.py || echo "⚠️ Vérification métriques échouée (continue)"

      - name: Generate UML Diagrams
        run: |
          VERSION=$(python3 -c "import re; from pathlib import Path; files = list(Path('.cursor/agents').glob('thebestclient*.md')); versions = [int(re.search(r'thebestclient(\d+)', f.name).group(1)) for f in files if re.search(r'thebestclient(\d+)', f.name)]; print(max(versions) + 1 if versions else 3)")
          python3 .github/scripts/generate-uml-diagrams.py $VERSION

      - name: Build Unity with Docker (Client + Server)
        continue-on-error: true
        run: |
          echo "🐳 Build Unity avec Docker..."
          echo "⚠️  Note: Unity nécessite une licence. Les builds peuvent échouer sans licence Unity."
          echo ""

          # Vérifier si Docker est disponible
          if ! command -v docker &> /dev/null; then
            echo "⚠️  Docker non disponible, skip des builds Unity"
            exit 0
          fi

          # Build de l'image Docker Unity
          echo "📦 Build de l'image Docker Unity..."
          docker build -t unity-6000.3.0f1-builder -f Dockerfile . || {
            echo "⚠️  Erreur build Docker image (peut nécessiter une licence Unity)"
            exit 0
          }

          # Test build Client (optionnel, continue si échec)
          echo ""
          echo "🔨 Test build Client..."
          docker run --rm \
            -v $PWD:/workspace \
            -w /workspace \
            unity-6000.3.0f1-builder \
            /usr/local/bin/build-unity.sh client || {
            echo "⚠️  Build Client échoué (normal sans licence Unity)"
          }

          # Test build Serveur (optionnel, continue si échec)
          echo ""
          echo "🔨 Test build Serveur..."
          docker run --rm \
            -v $PWD:/workspace \
            -w /workspace \
            unity-6000.3.0f1-builder \
            /usr/local/bin/build-unity.sh server || {
            echo "⚠️  Build Serveur échoué (normal sans licence Unity)"
          }

          echo ""
          echo "✅ Tests de compilation Docker terminés (peuvent avoir échoué sans licence Unity)"

      - name: Commit and Push Changes (to dev)
        if: github.event_name != 'pull_request'
        run: |
          set -euo pipefail

          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          git add -A
          if git diff --staged --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "🤖 Auto-improve: Cycle $(date +%Y%m%d-%H%M%S) [skip ci]"

          # Multiple scheduled/push runs can race and update dev concurrently.
          # Rebase & retry to avoid "rejected (fetch first)" and ensure changes land.
          for attempt in 1 2 3 4; do
            if git push origin dev; then
              exit 0
            fi
            echo "Push rejected; rebasing on origin/dev and retrying (attempt $attempt/4)..."
            git pull --rebase --autostash origin dev
          done

          echo "ERROR: failed to push to dev after 4 attempts"
          exit 1
